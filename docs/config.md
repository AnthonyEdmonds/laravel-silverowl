# Configuration

You can publish the SilverOwl configuration via `php artisan publish`

| Key        | Default                 | Usage                                 |
|------------|-------------------------|---------------------------------------|
| user_model | /App/Models/User::class | The User model used across the system |
